# Calcit 0.13.29 upgrade

- Resolved the upgrade branch against current `main` and updated the module snapshot version to Calcit 0.13.29.
- Kept `calcit.cirru` as the canonical snapshot and removed legacy `cr`/`compact.cirru` instructions from the workflow and README.
- Migrated CI to `setup-calcit` and current `calcit` commands.
- Verified dependency resolution, check-only validation, static reports, and JavaScript code generation.
